# jsbin — 更新日志

版本历史,最新在前。每条均经自举门 `gen2==gen3` 逐字节一致验证。

## v0.2.2(**test262 S1 首批(20.55% → 22.25%)+ 自编译 −19% + 串行自举门 — gen1==gen2==gen3 逐字节一致,fixtures 362/362,test262 22.25%**。**test262 S1 第一批**——五个 parser/compiler 修复把 COMPILE_FAIL **601 → 104**、新增 **+110 PASS(1328 → 1438 / 6462,20.55% → 22.25%)**(stride-5 子集):① **词法标识符 Unicode 转义**——`\u{...}`/`\uNNNN` 在标识符内解码(快路无转义直接 slice 零自编译开销 + 慢路);转义拼成保留字仍是关键字、在绑定位被拒,对齐 Node。② **非声明式 for-of/in 左值**——`for (a of x)`/`for ([a,b] of x)`/`for (a in x)` 可解析(顶层二元 `in` 后随 `)` 拆解为 for-in 左右值,零核心 Pratt 改动;数组/对象表达式左值由编译器重解释为 pattern)。③ **数组 rest 目标为绑定模式**——`[...[x]]`/`[...{length}]`/`function f([...[y]])`(对象 rest 按 ES `ObjectRestProperty = ...BindingIdentifier` 文法保持仅标识符)。④ **数组纯/尾 elision**——`[,]`/`[a,,]`/`[[,x]]`(elision 分支不再双吞闭合 `]`)。⑤ **对象解构触发 getter**——`var {a}=o`/`function f({x})`/`{[k]:v}` 在每次 `_object_get` 后补 `_maybe_getter`,访问器被调用而非绑定裸 getter 标记垃圾(直接成员访问经 IC 融合 `_maybe_getter` 本就正确)。**数据驱动发现(已入档)**:parser 缺口修复干净翻转 COMPILE_FAIL→可运行,但单点值 bug 修复转化低(getter 修复正确却翻转 0 用例——test262 用例常多重阻塞);剩余 CRASH 簇主因是**数组方法非泛型**(`Array.prototype.indexOf.call(new String(...))`/`.call({length,...})` SIGSEGV——数组方法假设真 0x7FFE 接收者),已定位为下一架构杠杆。**性能**(自编译 **25.5 → 20.7s user,−19%**):**原型键 IC(A3.5)**——读 IC 站点扩为 24B `{obj_shape, holder, index}`,直接原型键形状命中后经 holder 指针 + 键自验证证槽(深链回退不缓存),类方法/字段/constructor 键统一到 0x7FFC 驻留键世界(探针发现裸指针类键击穿所有键身份比较,慢路簇 ~12%→~6%);另 **`addString`/`registerRuntimeString` 驻留查找**(has+get→单 get,−4%)、**`resolveModulePath` 编译期缓存**(resolveImports 簇 6.7%→3.5%)、**`_strict_eq` 字符串融合比较**(Map 键探测省 2×strlen+memcmp,簇 6.5%→1.9%)。**基建**:`scripts/bootstrap-gate.sh`——串行(mkdir 锁)门,跑全链 `node→gen1→gen2→gen3`(先 `rm -f gen1 gen2 gen3`)、字节比对 `gen1==gen2==gen3`、断言 fixtures ≥362 且 FAIL==0。**文档**:README(中/英)自举口径修正为当前 ARM64-only 现状(x64 三目标定点在 v1.1.0 快照回退;devirt 禁用)。验证:gen1==gen2==gen3 逐字节一致,fixtures 362/362,test262 22.25%。)

## v0.2.1(**形状(隐藏类)IC 基础设施 + 自举"布局悬崖"根因修复 — gen1==gen2==gen3 逐字节一致,fixtures 362/362,test262 20.55%**。**对象形状 A1–A3.1**(docs/SHAPE_IC_DESIGN.md):对象头 48→56 字节,新增 `shape_ptr@48` 槽(0=无形状)。全静态数据键的对象字面量(排除 spread/访问器/计算键/重名/`__proto__`)与类实例(字段序按继承链展平、遮蔽去重,构造返回后经 `count==key_count` 运行时校验)获赋数据段静态形状描述符(仅 `{key_count}`——IC 只比较描述符**身份**)。属性读/写内联缓存(`_object_get_ic`/`_object_set_ic`)由 8 字节下标槽升级为 16 字节 `{cached_shape, cached_index}` 双模站点:形状指针命中即证明整个键序列,count/props 判空防御随之省去(键自验证单 cmp 保留作安全网——探针实测消除零增益,且它同时使 G-M-P 16B 撕裂对保持良性);无形状对象走与旧版逐字节一致的 legacy 路径。改键站点(append/delete/整数键重排/setPrototypeOf)统一把形状置 0。**本阶段性能刻意不变**(bench/prop ~8.7× vs Node;自编译 ~28s 原生 / 2.6s node 驱动——收益在其使能的形状键控方法 IC)。**"布局悬崖"根因破案**:间歇发作的 `gen1≠gen2` 定点失败(页对齐尾零)并非堆布局 miscompile——macOS `writeFileSync` 的 O_* 常量取 Linux 值,`O_TRUNC` 从未生效,旧的更大产物文件的尾巴原样残留;O_* 常量现按平台取真值(macOS `O_APPEND=0x8` 顺带转正),定点纪律新增"跑链前先 `rm -f gen1 gen2 gen3`",v1.x 时期的"~240KB TDZ 字符串发散"疑为同源假象。另修:A1 引入的 classinfo 手写头 8B 堆越界(48B 分配在 +48 写 shape_ptr)。原生剖析链路落地(macOS `sample(1)` + `LABEL_MAP` 符号化)——热点图记入 plan.md(编译器驱动 ~15%、属性慢路簇 ~12%、模块函数簇 ~17%、字符串内部 ~8%)。`--version` 现显示 `asm.js 0.2.1`。fixtures 362。)

## v0.2(**TypedArray 构造器全局值 + 动态方法分派 — test262 TypedArray 区自 0% 救起;CLI/CI 修复;.h 声明层决议**。头条:**TypedArray/ArrayBuffer 构造器现为真实全局值**——此前 `new Int8Array(...)` 是编译期特判、构造器从不以值存在,致 `harness/testTypedArray.js:106` 的 `ArrayBuffer.prototype.resize` 读 undefined 的属性、include 加载即抛,test262 整个 288 项 TypedArray 区归零。构造器现物化为 24B 闭包(`{magic, fnptr=_ta_ctor_tramp, type@16}`),配 per-构造器 `.prototype` 单例(`ArrayBuffer.prototype` 为空对象,include 的 resizable/immutable 特性探测安全返 undefined);`new TA(...)` 变量路径经专用 `_ta_construct`(无实例语义),覆盖 长度/数组/array-like/TA 源/ArrayBuffer 视图 全形态。**动态 TA 方法分派**:`new TA()` 未绑定 callee 现推 UNKNOWN(原 OBJECT 把 TA 实例落通用对象路径);unknown 接收者的 `join`/`arrayOnly`/`ambiguous(slice/at/indexOf/includes)`/`set`/`subarray` 成员调用按头字节 tag 分派到 `_ta_*` 实现——含两个门禁发现的正确性修复:头字节判别**必须只认裸指针**(0x7FFC 字符串内容首字节如 'A'=0x41 会冒充 TA 类型字节,曾击穿 base64/URLSearchParams/util fixtures 与自编译);接收者**单次求值**(`_evalOnceToIdent`——变异方法在 `ta.copyWithin(0,1).join()` 等链式调用中原被执行两次)。另修 `compileArrayMap` 把 TA 结果误装 0x7FFE(普通数组打印器把 buffer@24 当 data_ptr 解引 → 段错误)。test262 TypedArray 区:**0/288 → 10/288 PASS**(剩余缺口:%TypedArray% 原型链、方法值读取、描述符反射)。**CLI/CI**:`--version`/`-v` 显示 `asm.js 0.1`;`resolveModulePath` 不再把 `.js` 结尾的**目录名**(如改名后检出目录 `asm.js`)误判为文件(修 GitHub Actions 编译);Release 工作流产出 `asm.js-<target>`;全量 jsbin→asm.js/asmjs 扫描(417 处,含 `__jsbin_err`→`__asmjs_err` 全部按串名读者)。**治理注**:已知"布局悬崖"(plan.md 风险 #1)本周期实测**非确定性发作**——同一源码(含基线提交)定点在坏窗口翻 fail、好窗口翻 pass(编译后编译器内的堆布局运气,存量问题,根因待专项)。本条目验证:fixtures 362/362;定点链好窗口 3/3 全绿;TA repro 与 Node 逐字节一致。)

## v0.1(**公开仓库重新初始化 + 正式更名 asm.js — 版本号自 0.1 重新起算;能力基线 = 原 v1.5.52,非功能回退**。2026-07-19 项目重新初始化并正式更名:产品名 **asm.js**(主页 https://asm.js.cn),仓库迁至 github.com/qorm/asm.js(原 qorm/jsbin),历史重置为全新单提交,保留 `main`/`dev` 双分支,版本号自 **0.1** 重新起算。代码树与原 v1.5.52 基线逐字节一致(fixtures 362/362,macos-arm64 全链 gen1==gen2==gen3 逐字节一致)——这是**重新编号,不是功能回退**。新增 `--version`/`-v` 命令行开关显示本版本号。**LICENSE 由 MIT 变更为 GPLv3。**全部历史中的协作者署名信息已清除;清理前的引用快照另行离线保存(`jsbin-backup-2026-07-19.bundle`)。旧 v1.x tag 与 release 随重新初始化一并退役;下方 v1.x 条目保留作为重排前时代的开发档案。)

## v1.5.52(**G-M-P N>2 — 3+ OS 线程:通用工作窃取 + N 路停止世界 GC(linux-arm64)— gen1==gen2==gen3 逐字节一致,fixtures 362/362**。将多 M 运行时推广至 2 线程以上(docs/PARALLEL_DESIGN.md),单独落地。**P/M 注册表**(`P_MAX` 2→4)替换三个硬编码"另一个 P"站点:工作窃取(`_par_sched_run`)遍历所有 P 跳过自身;STW 收集器(`_gc_scan_other_ms`)遍历所有 P,跳过协调者与未 park 的 M,扫描每个 park 的 M 的 `[saved_sp, stack_hi)`;`_safepoint_poll` 恢复时清 `P.saved_sp` 保多周期正确。`_m_bringup_at(P)` + nprocs 参数化冒烟带起 **GOMAXPROCS=3**,STW 自旋中的 `sched_yield` 阻止请求者在 worker park 前饿死它们。经 Docker linux/arm64 验证(×15 绿):16 任务经通用工作窃取跨 **3 个真 OS 线程**排空(`m-sched3`),以及 **N=3 真停止世界 GC**——M0 停 M1+M2、扫描两个 worker 的 park 栈、keeper 活链表存活(`m-gc3`、`gc_count` 递增)。`GOMAXPROCS=1` 保持逐字节一致自举不动点(所有代码 linux-arm64/GOMAXPROCS 门控)。**注**:开发中踩了布局悬崖——把 N=3 冒烟复制为独立方法(+341 行)触发 __text 非确定性(约 1/3 概率翻转为 31 MB 空壳);经参数化去重(源码净减)恢复确定性。**剩余**:M6 channel 跨 M futex 唤醒(需共享协程路径);非 M0 协调者;分配压力 STW 自动触发;x64 段 TLS。fixtures 362。)

## v1.5.51(**NUL 透明字符串 + AES-GCM/SHA-512/HKDF + x64 process.exit/Date 修复 + 真 test262 harness(20.4%)— gen1==gen2==gen3 逐字节一致,fixtures 362/362**。**字符串现 NUL 透明**——三个独立 bug 修复:`_strconcat` 经 `_strcpy` 复制(遇首个 NUL 停)→ 现按已知长度 `_memcpy`(此即 PBKDF2/字符串内二进制损坏);`_strcmp` 在首个共享 NUL 处判等 → 现按长度;lexer 静默**丢弃** `\x00` 与 `\\u0000` 十六进制/unicode 转义的 NUL → 现保留(`\0` **八进制**转义仍丢,因自举编译器用 `"\0"` 作 EOF 哨兵)。`"a\x00b".length === 3`,索引/拼接/比较/JSON 现与 Node 一致。另 **`Buffer.concat`** 不再误派发到 `String.concat`(方法名冲突类)。**Node 加密**:AES-128/192/256-**CTR** + **GCM**(AEAD——GHASH 认证、`setAAD`/`getAuthTag`、篡改检测)、真 **SHA-512/384**(32 位分量对,无宽乘)+ 修复 NUL 截断摘要的字节 HMAC、**HKDF**(RFC 5869 向量)——全部与 Node 逐字节一致。**x64 后端**(arm64 发射全程字节一致):`process.exit()` 对所有 macOS 用裸 BSD 系统调用号,但 x64 需 Unix 类前缀(`0x2000001`)——class-0 系统调用在 Rosetta 下触发 `EXC_SYSCALL`,故每次 `process.exit` 崩溃;`_date_now` 跨 macOS 三参 `gettimeofday` 令 `A2`/RDX 持陈旧堆指针,内核把 mach 计数器写到活堆字上(`new Date()` 后任何属性写崩溃)。加上第 6 轮修复,交叉编译的 x64 编译器现在 Rosetta 下正确编译真实程序(完整 `cli.js` 自编译仍遇一个独立的布局敏感阻塞,已为后续映射)。**test262**:真一致性 harness(`tests/test262/run.mjs`)——YAML frontmatter/includes/负测试机制,逐个经 jsbin 编译运行——给出项目**首个诚实 test262 数字:1,318 / 6,462 = 20.4%** 的 stride-5 子集(`language/` + 13 个核心 `built-ins/`),并映射高杠杆失败簇(解析器宽松 ~1,030、内建属性描述符 528、一个损坏的 TypedArray include 归零 288、474 崩溃)。**WASM**:严谨负结果——尾调用段路由在 shipping 分层配置下实测 0.54–0.58×(更慢)(TurboFan 已优化直接调用蹦床);后端现在正确性/体积/性能全面收敛。fixtures 362。)

## v1.5.50(**G-M-P M5 完成 — 真多 M 停止世界 GC 周期(linux-arm64)— gen1==gen2==gen3 逐字节一致,fixtures 356/356**。完成 M5(docs/PARALLEL_DESIGN.md),单独落地(GC/协程热路径)。真垃圾回收现在多 M 执行期间运行:**mcache 尾哨兵 + pagemap 纪律**使各 P 的 mcache span 可清扫走查、可标记解析(`_mcache_refill` 镜像全局 span 哨兵模型);**安全点处 per-M 栈范围记录**(`P_G0_HI`/`P_STACK_HI`——各 park 的 M 栈顶从 `_scheduler_current` 捕获,在 park 时惰性做运行 G 栈区间,**不触碰协程引擎**——最高风险规避);以及**真 STW 收集器**(`_gc_scan_other_ms` 把根扫描扩展到每个 park 的 M 的栈,在 GOMAXPROCS>1 下 `_gc_mark_roots` 后调用,复用既有 drain/sweep/pacing)。多 M `_alloc` 不再内联触发 GC——回收必须 STW 协调。经 Docker linux/arm64 验证:`m-gc: real multi-M STW GC, keeper survived + tasks ok + gc ran`(×8 稳定)——keeper 活链表校验和存活回收、任务结果正确求和、`gc_count` 递增(真 GC 运行了)。单 M GC 不变(20 万对象 GC 压力程序在 node / macOS 原生 / linux-arm64 输出一致)。GC 现在 `GOMAXPROCS>1` 时启用。**剩余**(已记录):N>2 需 P/M 注册表 + 协调者选举(当前扫描假设 M0 协调者 + `_p_array[1]`);分配压力自动触发 STW;x64 段 TLS。`GOMAXPROCS=1` 保持逐字节一致自举不动点(所有 M5 代码 linux-arm64 / GOMAXPROCS 门控)。fixtures 356。)

## v1.5.49(**x64 运行时正确性(数字格式化 + filter/JSON/for-in 崩溃)+ 函数种类/名称反射 + AES 密码 + http keep-alive — gen1==gen2==gen3 逐字节一致(macos-arm64),fixtures 356/356**。**x64 后端正确性**(arm64 发射全程字节一致;每项修复 `vm.arch==="x64"` 门控):一簇 `V0`/`RET`→`RAX` 寄存器别名 bug——`value>>48` 标签计算破坏装箱接收者——修复了 **Dragon4 数字格式化**(`42`/`255`/`123456` 曾截断为 `40`/`200`/`100000`)、**`Array.prototype.filter`**(返回裸数字 → 使用时 SIGSEGV)、**对象的 `for…in`/`JSON.stringify`**(解引用裸标签 `0x7ffd` → SIGSEGV)。Rosetta 2 下,x64 现在数字、filter/map/reduce、JSON、`Object.keys/values/entries`、for-in 及 22 例电池全部与 arm64 匹配。(x64 自举现在远超此前崩溃点——`gx1` 编译不再段错误——但尚未达到不动点;剩余下游 x64 缺陷已为后续清扫映射。)**ES**:函数元数据侧表(`code_ptr→{种类,名称}`,启动时构建,置于 GC 根区之后故从不扫描)驱动 **GeneratorFunction/AsyncFunction/AsyncGeneratorFunction** `toString` 品牌与运行时函数值(参数、成员链)的 **`fn.name` 反射**,不仅静态访问点。**Node**:真 **AES-128/192/256-CBC** `createCipheriv`/`createDecipheriv`(纯字节 AES + GF(2⁸) 农夫乘 + PKCS#7)与 **PBKDF2**——密文 + 派生密钥经与 Node 逐字节验证;**http keep-alive 连接池**(顺序同主机请求复用一个 TCP fd,`reused=2 created=1`,干净退出);真流式 **zlib** Transform 类。**WASM**:严谨的负结果——段本地寄存器缓存实测为决定性回归(寄存器文件必须跨每个段边界逐位保留,迫使每次跨越全刷新/重载),已拒绝并记录(真杠杆是跨段派发成本)。fixtures 356。)

## v1.5.48(**G-M-P M4 + M5 — 并行分配 + 停止世界安全点基础设施(linux-arm64)— gen1==gen2==gen3 逐字节一致,fixtures 351/351**。延续多核轨道(docs/PARALLEL_DESIGN.md),单独落地(GC/分配器热路径)。**M4 — per-P 无锁分配**:每个 P 获得线程本地 mcache(从共享 mheap 切出的 bump 区),故第二个 M 上的 `_alloc` 从其 P 的 mcache 取,快路径无锁;慢路径加锁重填(`mcentral` 式),大对象在同一锁上串行。经 Docker linux/arm64 验证:16 个分配任务跨 2 个 OS 线程(各建 64 节点链表)经工作窃取排空,确定性校验和(`m-alloc sum=8704`,×12 稳定)。**M5 — 协作安全点 + 停止世界**:每个 M 在调度器循环与 mcache 重填边界轮询 `_stw_requested` 标志;请求时把 SP 存入其 P、原子 park(futex,先检查后睡)、唤醒后恢复;请求者置标志、等所有其他 M park、独占世界、再唤醒它们。已验证:`m-stw: STW park/resume roundtrip across 2 Ms`——真停止世界暂停确实发生,校验和存活。`GOMAXPROCS>1` 时 GC 仍禁用(活多 M 回收需协程栈扫描 + mcache 清扫可走性——剩余 M5 工作,设计文档已精确划定)。`GOMAXPROCS=1` 保持逐字节一致自举不动点(所有 M4/M5 代码 GOMAXPROCS 门控,仅 linux-arm64 发出)。fixtures 351。)

## v1.5.47(**全部 fixture 全绿(351/351):跨模块标签冲突修复 + 异步 HTTP/UDP + 一等数组迭代器 + 循环 require + wasm −12.6% — gen1==gen2==gen3 逐字节一致,fixtures 351/351**。两轮 agent 合并收割;2 个长期失败的 fixture 现已修复,测试套件**首次全绿**。**编译器正确性**:**跨模块标签冲突**——不同模块中两个同名类(如 `net`/`http` 的 `Server`)发出相同的构造器/方法局部标签,汇编器静默覆盖,故一个类体内的跳转解析进另一个类的代码(错类构造/无限递归)——经把各类全局唯一 id 混入其标签上下文修复(字节中性,经移除 `http.js` 的 `Server`→`HttpServer` workaround 验证);另加类方法捕获 `let`/参数共享(box-on-capture 现对类方法运行)、`null[k]`/`undefined[k]` 抛可捕获 TypeError(镜像 `.x` 修复)、`String.split` 支持多字符分隔符、`export { a, b, }` 尾逗号解析。**循环 `require()`**:参与 require 循环的本地 CJS 模块现编译为独立惰性初始化函数,在体入口发布 `module.exports`,故循环再 require 读到部分对象(Node 语义)——修复 2 个此前失败的 fixture;ESM/无环 CJS 不变。**Node 网络**:事件驱动异步 **`net`**(骑在 `setImmediate` 队列上的 poll(2) 就绪泵——脆弱的 `_ev_run` 汇编未动),带真 `'data'`/`'connection'`/`'end'` 事件;最小真 **HTTP/1.1** 服务端 + 客户端,双向 chunked 传输编码 + keep-alive 管道;以及 **`dgram`/UDP4**(经 `sendmsg`/`recvmsg`,绕开 5 参 syscall 上限)。**ES**:一等**数组迭代器**(`arr.values()`/`.keys()`/`.entries()`/`[Symbol.iterator]()` 为带 `next()` 的真迭代器对象);WeakMap/WeakSet `toStringTag`;直接 eval **逃逸闭包变异**(从 `eval` 返回的闭包看到调用者后续对捕获变量的写)。**WASM**:四项编码/体积优化(memarg 偏移折叠、sleb `-1`、自适应 br 深度填充、收窄 a64 地址常量)——**hello.wasm −12.6%、cli.js→wasm −15.6%**,保持确定性逐字节一致输出。**x64**:三项后端寄存器别名修复(交换律运算 scratch 破坏、Dragon4 `_d4_shl`/`_d4_add3` 别名)——arm64 字节一致;一个预存的 x64 数字格式化缺陷簇待专项清扫(已记录)。fixtures 351。)

## v1.5.46(**G-M-P M3 — 跨 2 个 OS 线程的真多 M 调度(linux-arm64)— gen1==gen2==gen3 逐字节一致,fixtures 332/334**。项目首次真多核执行。单独落地(共享热路径)。构建真 Go 风格调度器基座(所有活体门控到 linux-arm64,他处为桩):**ARMv8 LL/SC 原子**(`ldaxr`/`stlxr`/`clrex`/`stlr` 经 asm→backend→vm)、自旋锁、**per-P 运行队列**(环形 deque)、加锁全局队列、原子 runnable 计数器,以及 `_par_sched_run` 循环(本地 → 全局 → 从另一 P **工作窃取** → futex 空闲,Go 的 find-runnable 形态)。`_scheduler_spawn` 在 `GOMAXPROCS>1` 时路由到并行 spawner;第二 M 引导预置其 P/g0/调度器指针并进入并行循环。经 Docker linux/arm64 验证:抬升 `GOMAXPROCS=2`,16 个运行至完成的协程派发到 P0,**两个 M 经工作窃取排空**,以确定性校验和 join——8 次运行稳定(`16 tasks across 2 Ms, sum=136 ok`)。`GOMAXPROCS=1` 保持逐字节一致自举不动点(多 M 代码 GOMAXPROCS 门控,默认从不进入)。**范围**:第二 M 分配安全(mcache 竞争)属 M4/M5——冒烟用非分配任务 + `GOMAXPROCS>1` 时关 GC,按设计过渡策略;跨 M channel 唤醒属 M6;x64/wasm 发桩待段 TLS。发现并修复一个 codegen 陷阱:`asm/arm64.js` 把任何 `_global_` 前缀标签当数据标签,故名为 `_global_runq_pop` 的函数崩溃——队列标签改名 `_grq_*`。fixtures 332。)

## v1.5.45(**真 zlib + 真 TCP、TypedArray toString/打印/copyWithin 修复、类 Symbol.iterator 派发、eval 闭包写回 — gen1==gen2==gen3 逐字节一致,fixtures 332/334**。**Node**:真 **zlib** — 真 DEFLATE/INFLATE(stored/fixed/动态 Huffman + LZ77)、gzip/zlib 容器带 CRC32/Adler-32、`deflateSync`/`inflateSync`/`gzipSync`/`gunzipSync`/`unzipSync`(自动检测),验证与 **Node 双向往返**;以及真 **TCP** — `net.createServer`/`connect` 经裸 socket 系统调用(socket/bind/listen/accept/connect,macOS + linux arm64/x64)、同步 loopback + `isIP*`,亦**经 Node 互操作验证**(异步事件驱动 socket 仍需事件循环内 epoll/kqueue——延后)。**ES codegen/运行时**(多为 WASM 差分暴露的崩溃):`String(typedArray)`/`` `${ta}` ``/`ta.toString()` 给出垃圾浮点(裸堆 TypedArray 指针被当 float64 读)——现走 `_ta_join` → `"1,2,3"`;`Object.prototype.toString.call(ta)` 在原生**段错误**(品牌表仅 wasi;原生野解引用头)——现在所有平台品牌 TypedArray/ArrayBuffer/DataView/BigInt/Symbol/Promise;`console.log(typedArray)` 打印 `[object Object]`、`console.log(promise)` **段错误**——现为 `Uint8Array(3) [ 1, 2, 3 ]` 与短路 Promise 打印;类体 `[Symbol.iterator]`/`[Symbol.asyncIterator]` 未被 `for-of`/`for await` 派发(存于符号键但按字符串键读)——统一到字符串键(hasInstance/toPrimitive/toStringTag 仍在符号键);`TypedArray.prototype.copyWithin` **段错误**(落入普通数组布局路径)——新 `_ta_copywithin` helper。**引擎**:eval 内非逃逸闭包现**写回**捕获的调用者标量(`eval("[1,2,3].forEach(v=>{ s=s+v })")` → 6),经 copy-in 槽的 rebox-on-capture 走共享堆 box。**WASM**:Generator/AsyncGenerator 对象品牌 `[object Generator]`/`[object AsyncGenerator]`(经 `__gen_coro` 内部槽判别)。fixtures 332。)

## v1.5.44(**G-M-P M2 完成 + M3 第二 M 引导准备 — gen1==gen2==gen3 逐字节一致,fixtures 322/324**。延续多核地基(docs/PARALLEL_DESIGN.md),单独落地(共享热路径)。**M2 完成**:剩余的活 per-M 执行状态槽现经 arm64 `x28` P/M 寄存器上下文化——`_gen_last_coro`、`_parse_lenient`、`_print_buf`(首个*热路径*指针化槽:整数打印增一次解引用,输出验证与 Node 逐字节相等)。这完成了 v1.5.41 开始的活 D 组(`_call_argc`/`_exception_*`/`_exc_ctx_top`);仅剩 `_stack_base`,延后至 M5 协程栈扫描 pass。修正一个 round-1 文档前提:`_call_stack`/`_call_stack_top` 实为**死代码**(`ErrorGenerator` 从不实例化——活 `throw` 路径把 `new TypeError` 编译为普通对象),故其指针化被回退、§1.2 划除。**M3 准备**(仅引导管线):新叶模块加第二 M 线程入口蹦床(`bindMContext` → `_scheduler_run` → 线程退出)+ GOMAXPROCS 门控的引导路径,基于既有免 libc `clone()`/`futex` 原语。`_gomaxprocs` 默认 1 且 AOT 产物无处抬升,故引导代码在单 M 不动点下从不进入——纯增量死代码。冒烟测试在 Docker linux/arm64 **与** linux/amd64 通过("second M joined");macOS 返回设计的桩。真多 M 调度/窃取 + M>1 分配/GC 安全仍属 M3–M5。fixtures 322。)

## v1.5.43(**Node 流 + child_process + 定时器、ES codegen 修复、eval 捕获对象变异、WASM 类型品牌 — gen1==gen2==gen3 逐字节一致,fixtures 322/324**。**Node 核心**:真 **流**(Readable/Writable/Duplex/Transform/PassThrough 带背压、`pipe`/`pipeline`/`finished`、对象模式、`Readable.from`)替换空桩;fs `createReadStream`/`createWriteStream` + `appendFileSync` 截断修复;异步 **`child_process`** `spawn`/`exec`/`execFile` 带 stdout/stderr 流与有序 `exit`/`close`;`setInterval`/`clearInterval`。**ES codegen 正确性**(Node 工作暴露的五个 bug):`typeof new Map()`/`new Set()` 返回 `"number"`——裸堆指针在 `_js_typeof` 落入 double 区间判定;现走堆内类型标签分支返回 `"object"`。类方法字面命名 `end`/`return`/`proto` 令 `main` 无限重跑——构造器结构标签与方法体标签冲突;重排使末尾 token 永非方法名。参数传入的 **RegExp** 上 `.test()`/`.exec()` 崩溃——接收者丢失静态 regexp 类型;现在 shim 存在时走运行时 `__isRegExp` 分支(带自有 `test`/`exec` 的用户对象仍正常派发)。`fn instanceof Function`/`Object` 为 false——裸 `Function` 未映射;映射为哨兵 + `_instanceof` 接受函数标签/闭包(原语仍 false,经参数可用)。**`setTimeout` 回调内**解决的 Promise 从不排空 `.then`——reaction 队列未在定时器微任务后于 `_ev_run` 内抽取;现循环至调度器与 reaction 队列皆空。**引擎(route B)**:**变异捕获的调用者局部数组/对象**的直接 eval 现经共享堆传播(`eval("a.push(3)")`、`pop`/`shift`/`splice`、`o.k=9`、下标写)、eval 内闭包可读捕获变量(`let base=10; eval("[1,2,3].map(v=>v+base)")` → `[11,12,13]`)——经在引擎符号表注册数组变异器 + 装箱运行时符号。**WASM**:TypedArray/ArrayBuffer/DataView/Promise/BigInt/Symbol 的 `Object.prototype.toString` 品牌(修复 DataView/BigInt 的真 wasm OOB **陷入**)+ BigInt 值字节标签冲突修复。fixtures 322。)

## v1.5.42(**直接 eval 词法作用域捕获 + 真加密哈希 + Node 核心深度 + ES/WASM 正确性 — gen1==gen2==gen3 逐字节一致,fixtures 309/311**。**引擎(route B)**:函数内直接 `eval(str)` 现**读取并改写调用者的局部变量**(`function f(){ let x=10; eval("x=20"); return x }` → 20)——运行时 eval 引擎的最后一块硬骨头;调用点把外层帧的 名→槽 布局 + 活 FP 序列化进片段,片段入口拷入调用者槽、返回前拷出(间接 eval——`(0,eval)`、别名、成员形式——保持全局作用域)。同时修复一个潜伏回归——它在当前构建上曾令**所有**运行时 eval 崩溃:"Design A" TypedArray 布局把元素数据移到 `data_ptr@16` 之后,但引擎 exec 内建仍在 `block+16` 读片段字节(把指针当机器码 → SIGILL)——因编译器不自 eval,自举门看不到。**Node 加密**:真 **SHA-256 / SHA-1 / MD5 + HMAC**(纯 32 位整数)替换假 djb2 桩。**Node 核心深度**:buffer(base64url、变长 `read/writeUIntLE/BE` + 有符号、迭代器、toJSON)、assert(`AssertionError.code`、match/doesNotMatch/ifError、`throws` 匹配器、`assert.strict`)、url(相对基址解析 + RFC-3986 点段 + `pathToFileURL`)、os(userInfo/networkInterfaces/machine/version/availableParallelism)、events(prependOnceListener、未处理 `error` 抛出、`once()` Promise、getEventListeners/setMaxListeners)、string_decoder `.end()`、util.inspect(Map/Set 大小+嵌套、Date ISO、`[Function: name]`)。**ES 正确性**:TypedArray `toReversed`/`toSorted`/`with`;引擎合成的 TypeError 为真 `TypeError` 对象;计算键解构赋值运行时求值键;类原型获得 `constructor` 反向引用;可选链私有字段 `o?.#x`;`Promise` 无 `new` 调用抛错。**WASM**:i64 `INT64_MIN / -1` 除法溢出守卫;影子栈 4 MB→16 MB(匹配原生递归深度)。fixtures 309。)

## v1.5.41(**G-M-P 多核地基:M2 — P/M 上下文化 + per-M 槽 TLS 化(arm64 x28)— gen1==gen2==gen3 逐字节一致,fixtures 292/294**。迈向真共享堆并行的基建里程碑(docs/PARALLEL_DESIGN.md,M2),**单独落地**——因其改写自举编译器依赖的热路径。保留 arm64 **x28** 作 P/M 上下文寄存器,加 per-M 结构(GOMAXPROCS=1 下单个静态 `_m0_context`,置于 GC 扫描数据区),`_start` 处绑定。per-M 执行状态槽经单一后端拦截点从固定全局地址移入 M 上下文——把其 `lea` 改写为 `x28` 相对 `add`,零调用点改动:整套**异常/展开机器**(`_exception_pending`/`_exception_value`/`_exc_ctx_top`——try/catch/finally/rethrow,以及 L2 引擎符号表)与**最热 ABI 槽** `_call_argc`(每调用点均写)现经 x28 读取。因数千调用点 `lea`(2 指令)→`add`(1 指令),**编译器二进制缩 ~164 KB(15.19 → 15.02 MB)**,同时保持逐字节一致不动点——在最热槽上验证了设计"x28 寻址净赚"的预测。M1 并发(`js f(x)` 派发 + Channel、未捕获 panic)端到端验证完好。**范围**:仅 arm64(x64/wasm 在 GOMAXPROCS=1 下保留平坦全局——段寄存器 TLS 延后);`_scheduler_current`(GC 推迟判据)延后至 M5 协程栈扫描 pass;两个 D 组槽(`_call_stack`、`_print_buf`)发现为内联缓冲,需先指针化再上下文化。fixtures 292。)

## v1.5.40(**Go 风格并发方言(`js f(x)` + Channel)+ WASM 分段 CPU + 一等 intrinsic + Proxy/函数值修复 — gen1==gen2==gen3 逐字节一致,fixtures 292/294**。**并发(Stage 0)**:一项刻意的 ES 方言扩展——语句位的 `js <调用>` 把该调用作为协程派发到事件循环(上下文关键字,零歧义——两个相邻标识符从不是合法 JS,故 `const js=1`/`js(x)`/`js.m()` 保持普通),配套 `Channel(capacity)` CSP 内建(会合 + 缓冲 send/recv/close,骑在 promise/协程 park-wake 原语上)。fire-and-forget;派发协程中未捕获异常 Go 风格 panic(exit 1)。真线程铺垫:linux 免 libc 裸 `clone()`/`futex` 原语,以及完整多核 **G-M-P 设计研究**(docs/PARALLEL_DESIGN.md——共享堆、直奔 Go runtime 的里程碑路线,不绕隔离堆)。**WASM**:**M3 分段虚拟 CPU**——整程序单 wasm 函数后端拆为按段函数并重启分层(解除单函数上限),建立在 **M2 syscall surface**(argv/env 注入、fs、random、WASI 适配行)与 wasm fixture 运行模式(`jsbin run --wasm`)之上。**一等 intrinsic(第二波)**:`Array.isArray` 与内建命名空间 statics(`Math.*`、`Object.keys/values/entries`、`Date.now`)现为可提取/别名/传递的具现值,不再仅是调用点。**正确性**:生成器 `return()`/`throw()` 现运行挂起的 `finally` 块(按协程异常链);`Object.prototype` 方法作值提取 + `isPrototypeOf` arm64 崩溃;函数 `.name`/`.length` 描述符反射 + `defineProperty` 覆盖可见性;运行时函数值(参数/变量)上 `new` + Proxy `construct` 转发到普通函数;可调用 Proxy 作数组方法回调 + 函数下标键(`f[sym]`)。另加重写的 ES_SUPPORT.md 支持矩阵。fixtures 292。)

## v1.5.39(**WebAssembly 目标 + 调用 ABI argc(完整 Proxy、真 arguments.length)+ 属性容器 — gen1==gen2==gen3 逐字节一致,fixtures 281/283**。两项结构性里程碑外加一个新编译目标。**WASM**:`--target wasm32-wasi` 把用户程序编译为 WebAssembly(经 `node scripts/wasm_host.mjs prog.wasm` 运行)——手写零依赖 wasm 模块 writer + "单函数虚拟 CPU" 后端(整程序一个 wasm 函数,pc 派发 `br_table`;虚拟寄存器为 i64 locals;线性内存影子栈使保守 GC 零改动;syscall 走宿主 import)。hello/字符串/闭包/try-catch/生成器/Map/Set/JSON/async-await/GC 压力全部与原生逐行一致(设计:docs/WASM_DESIGN.md;~4.5× 原生耗时,M2:fs/argv/WASI 适配)。**调用 ABI argc**:每个调用点设置 `_call_argc` 约定——`arguments.length` 对真 `undefined` 实参正确;**Proxy `apply`/`construct` 陷阱**可用(可调用 proxy:`p(1,2,3)` → argsArray `[1,2,3]`、`new p(...)` 带 newTarget、`typeof callableProxy === "function"`)——Proxy surface 现基本完整;另加 `new (复杂表达式)()`。**属性容器**:GC 根侧表使函数可带自定义属性(`fn.x = 1` 覆盖所有函数值形式含别名/调用结果)、修复函数声明的 `f === f` 身份(此前为 false!)、并支撑通用自定义 tag `` tag`...` ``.raw(按站点模板对象身份)。**求值顺序**:`o()[k()] = v()` 与 `{[k()]: v()}` 现严格从左到右,复合赋值不再对基座求值两次(纯操作数站点保持字节一致 codegen)。另加 for-of `break` 调用 `iterator.return()`(IteratorClose)、Proxy 不变量强制。fixtures 281。)

## v1.5.38(**记分卡驱动的 ES 正确性 — 描述符、枚举顺序、类语义、Proxy 陷阱、RegExp — gen1==gen2==gen3 逐字节一致,fixtures 272/274**。由 es-compat 记分卡指导的九项修复(记分卡也修正了一个策略误读:"属性描述符子系统"已建成,非 111 测试缺口)。**对象/描述符**:ES `[[OwnPropertyKeys]]` **枚举顺序**(整数键升序,再字符串插入序)横跨 keys/values/entries/for-in/JSON/assign——带零变更"已排序?"快路使编译器字符串键对象字节不变;描述符 **delete 强制**(`configurable:false`/sealed/frozen 删返 false)+ `Reflect.preventExtensions`。**类语义**:`class extends (expr)`(值基超类——任意表达式/mixin/条件,super 可用);跨 `extends` 的**静态方法/属性继承**(`B.f()`、`getPrototypeOf(Sub)===Super`);**派生字段初始化顺序**(字段在 `super()` 后运行,故 `class C extends A{ b=this.a }` 读超类设置的状态);**计算访问器方法** `get [expr](){}`。**Proxy**:getOwnPropertyDescriptor/defineProperty/ownKeys/preventExtensions/getPrototypeOf **描述符陷阱**(surface 现完整除 apply/construct)。**函数**:用户函数 `.name`/`.length` 具现(访问点静态解析)。**解析器**:解构赋值默认值(`({a=1}=obj)`)、赋值作调用实参(`f(o.x ||= v)`)。**RegExp**:`d` 标志 + 匹配 `.indices` + `hasIndices`、内联修饰组 `(?i:...)`(ES2024)、`replace(re, fn)` 命名组参数。诚实 es-compat 覆盖 39.6%→41.7%。fixtures 272。)

## v1.5.37(**async 补全 + 非 ASCII 字符串修复 + es-compat 记分卡修复 — gen1==gen2==gen3 逐字节一致,fixtures 259/261**。**async 现覆盖所有函数形式**:**async 方法**(类 + 对象字面量)返回 Promise(此前 `obj.m().then()`/`.catch()` 仅靠运气——方法返回裸值);**`for await` 遍历内部 await 的 async 生成器**(经 `Symbol.asyncIterator` 键归一)——接上 v1.5.36 的 async 生成器 + async 可迭代。**shipped-compiler 正确性**:**非 ASCII 字符串字面量**现在**编译产物**中发射正确 UTF-8——自编译编译器曾对 CJK/重音/emoji 双重编码(`你好`→乱码,node 运行输出本正常);经一致的字节串模型修复(latin1 源读 + 透传发射 + `_cpToUtf8` 转义展开),对 raw/`\x`/`\u`/`\u{}`/surrogate 各形式(含模板字面量内)逐字节匹配 node。此**仅编译产物**的 bug 类现由自举 probe 矩阵守卫(10+ probe 经自编译二进制编译+运行——ESM fixture 门结构上看不到仅编译产物分歧)。**es-compat 记分卡修复**:裸 `return` 不再吞掉下一语句或返回 int `0` 而非 `undefined`;访问器上逻辑复合赋值(`o.x ||= v`)短路时不再调 setter;`Map`/`Set` 归一 `-0`→`+0` 键(SameValueZero);`Array.from(x, mapFn, thisArg)` 绑定 thisArg。(完整 es-compat 记分卡:诚实 ES 覆盖 ~46%,识别**属性描述符**为单一最高杠杆剩余子系统——现为下一 infra 焦点。)fixtures 259。)

## v1.5.36(**async 生成器 + async 可迭代 + `String.raw` + 自举 probe 矩阵 — gen1==gen2==gen3 逐字节一致,fixtures 252/254**。**async 迭代**:`async function*` async 生成器——yield 与 await 在一个协程共存(独立 `coro+72`/`coro+88` 槽),`.next()` 返回 `Promise<{value,done}>`;经手动 `.next().then()` 驱动。另加 `for await (x of asyncIterable)` 遍历 `Symbol.asyncIterator` 对象(Phase 2a;async 迭代器协议路径,无协程原语)。**`String.raw`** 标签模板——lexer 现串接原始(未转义)quasi 文本;解析期脱糖为原始 quasi 链。**正确性**:**函数内**定义的对象字面量简写方法现取动态接收者,而非错误捕获外层词法 `this`(曾坏工厂/迭代器模式;类实例/模块 mixin 不受影响故自编译中不显);`console.log` 名如内建的用户方法(`.test`/`.has`/`.includes`/`.startsWith`)不再误渲染为布尔。**测试基建**:自举 probe 矩阵——10 个 probe 经**自编译 g1** 编译+运行小程序——捕获 ESM 门结构上看不到的"编译产物分歧"类(已借此发现一个 shipped-compiler 非 ASCII 字符串 bug,修复中)。(延后:`for await` 遍历内部 await 的 async 生成器;编译程序中的非 ASCII 字符串字面量;fs。)fixtures 252。)

## v1.5.35(**`require()` 自举修复 + async 迭代 + `with` + `child_process.execSync` + 全局 — gen1==gen2==gen3 逐字节一致,fixtures 247/249**。**自举正确性**:`require(内建)` 在编译产物中返回正确导出类型——前导斜杠判据(`indexOf("/runtime/node/")`)仅匹配 node 的**绝对**路径,自编译编译器相对解析路径→ESM shim 误判命名空间→`require("events")`/`new EE()` 在每个编译程序中崩;修为路径格式无关,并加 **g1 编译的自举 probe** 守卫整个"编译器产物分歧"类(ESM fixture 门无法覆盖)。**`for await (x of iterable)`** 遍历同步可迭代 + promise 数组(Phase 1——复用 await+for-of,无新协程原语;async 生成器延后至协程 pass)。**`with (obj) {}`** 语句——完整读/赋值/更新/方法解析,门控使非 `with` 代码字节不变。**`child_process.execSync`/`spawnSync`**——真同步子进程 + stdout 捕获(fork + 临时文件重定向 + execve + wait4;绕开 macOS x0/x1 寄存器返回怪癖;raw-safe 缓冲读)。**全局**:`URL`/`URLSearchParams`/`btoa`/`atob` 经隐式导入注入。另加 **`arr.unshift(...spread)`**。(fs 仍延后——其自编译损坏现证为深层 `_number_coerce` 误读裸堆值的脆性、由 fs 堆扰动触发,非裸指针算术站点。)fixtures 247。)

## v1.5.34(**Proxy + typed-array 多视图补全 + util/timers — gen1==gen2==gen3 逐字节一致,fixtures 240/242**。**Proxy**:`new Proxy(target, handler)` 带 `get`/`set`/`has`/`deleteProperty` 陷阱 + 转发 + 枚举(`Object.keys`/`values`/`entries`/`for-in`/`JSON.stringify`)+ 静态与动态属性访问——经独立 `TYPE_PROXY` 字节自动漏判快路对象检查,普通对象访问逐字节不变、gen1==gen2==gen3 天然保持(apply/construct 陷阱待调用 ABI argc 后补)。**typed array 补全**:缓冲支持的多视图(`new Int32Array(buffer, byteOffset, length)`)——一个 ArrayBuffer 上的多个 typed 视图 + DataView 共享写入(GC 追踪);`.buffer` 稳定 `===` 身份、`.byteOffset` 正确。**Node**:`require("util").format`/`inspect`/`promisify`/… 现顶层导出;裸全局 `setTimeout`/`setImmediate`/`clearTimeout` 无需 import 即触发。(再次延后:`fs`——其原始 stat 缓冲指针算术会被运行时 `_number_coerce` 误判为装箱 Number 指针,按堆布局**非确定性**损坏自编译;部分修复杀掉段错路径但仍存错误输出表现,故 fs 暂出,待完全确定性。)fixtures 240。)

## v1.5.33(**编译器 −6.68 MB(−34%)+ 最短往返 float + ArrayBuffer/DataView + GC 屏障 — gen1==gen2==gen3 逐字节一致,fixtures 234/236**。Footprint:自编译中在**每个方法/闭包调用点——36,603 站点**内联发射的可调用校验(~30 指令)消融为单条 `bl _validate_callable`(callee-saved S4 scratch,x64 发射逐字节相同),产物 **19.87 MB → 13.30 MB(−6.68 MB,−34%)**,~6.8 MB 余量;全 5 目标干净。**正确舍入的最短往返 float**:`_floatToString` 曾发垃圾(`1234.5`→`"<34.5"`、`1e20`→INT64_MAX、无指数);换成 VM 汇编的 32 位 limb **Dragon4**——对 node 11,190 例 + 1,050 次正规数 0 失配(`0.1+0.2`→`0.30000000000000004`、`1/3`→`0.3333333333333333`、`1e21`→`1e+21`,往返成立),确定性安全(编译器只格式化整数)。**ArrayBuffer/DataView**:`ArrayBuffer.byteLength`/`.slice`、`TypedArray.buffer`/`.byteOffset`(惰性包裹缓冲)、及 **`DataView`** get/set(Int8…Float64 × 端序),DataView↔typed-array GC 下真实共享内存。**GC 正确性**:`_array_unshift`/`splice`/`fill` 现发射分代写屏障(`_gc_remember`)——修年轻值存入老数组的损坏。(仍延后:`fs`,受字节布局特定的潜伏堆损坏阻塞——现证尺寸无关、非 ~20MB 区——排查中。)fixtures 234。)

## v1.5.32(**完整编译器确定性(gen1==gen2==gen3)+ Symbol/typed-array/ArrayBuffer/Math 正确性 — 13 项修复,逐字节一致,fixtures 229/231**。头条是确定性里程碑:**`node cli.js`、自编译编译器、自举编译器现发射逐字节相同输出(gen1==gen2==gen3)**。最后的非确定性残差是 jsbin 字节导向字符串引擎与 V8 在两种字面量形式上的分歧——内嵌 NUL(jsbin NUL 结尾)与非 ASCII UTF-8(jsbin 读原始字节→双重编码);二者已修(编译器源码字符串字面量现 ASCII/NUL 洁净),并修复 ELF 字符串表写入。正确性批:**`Symbol.toPrimitive`**(全 hint)、**`Symbol.hasInstance`**(自定义 instanceof)、**`Symbol.toStringTag`** + `Object.prototype.toString` 标签、**`Class[symbol]`** 下标读写 + 静态 symbol 字段、**`Reflect.construct`/`getOwnPropertyDescriptor`**、**`Uint8ClampedArray`** 饱和、普通函数 **`new F(...spread)`**、**typed-array `map`/`filter`/`values`/`entries`** 同类型 + 闭包派发、**`Map`/`Set` NaN 键**(SameValueZero)、**回调 `thisArg`**、**`ArrayBuffer.byteLength`/`.slice`**、**`console.log(-0)`**、**`(-0).toFixed`**、**Math log 定义域守卫**、及 **~25 个 Math 函数** 不再把硬件 NaN 打印为 `0`/`1`。(已知:`fs` 已证可运行但暂缓,其门槛 ~20MB 堆布局不稳定正根因排查中;float 打印尚非最短往返。)fixtures 229。报告见 `tests/es-compat/`。)

## v1.5.31(**布局确定性根因修复 + footprint −768 KB + Symbol/typed-array/Reflect 正确性 — gen2==gen3 逐字节一致,fixtures 216/218**。里程碑是确定性修复:编译器把 `import.meta.url` 作**绝对、cwd 依赖**路径串内嵌,故**同源在不同目录编译出不同 `__data` 布局**——长期"某路径干净、另一路径误编译"非确定性的根。现归一为 cwd 无关 basename(已验:任意路径编译产物逐字节一致),另加配套 `#32` 族修复(编译器守卫不再把 `Set.prototype.has` 当值读)。第三个 footprint 杠杆——payload-mask `AND` 经后端分派原语(arm64 单条位掩码立即数;x64 发射逐字节相同、机械证明)——再回收 **768 KB**(三轮累计 ~1.28 MB;产物现 ~19.5 MB、~700 KB 余量)。正确性:**`Symbol.toPrimitive`**(全 3 hint)、**`Symbol.hasInstance`**(自定义 `instanceof`)、**`Symbol.toStringTag`** + `Object.prototype.toString` 品牌标签、**`Reflect.construct`/`getOwnPropertyDescriptor`**、**`Uint8ClampedArray`** 饱和、普通函数 **`new F(...spread)`**、**typed-array `map`/`filter`** 返回同类型 + `instanceof`。(收官进行中:正确的最短往返 float→string 将闭合最后的 `gen1≠gen2` 残差并解锁 `fs`。)fixtures 216。报告见 `tests/es-compat/`。)

## v1.5.30(**编译器 footprint −512 KB(跨后端)+ 方法引用补全 + Date/Number locale + regex/Date 正确性 — gen2==gen3 逐字节一致,fixtures 208/210**。第二轮 footprint 消融回收 **~512 KB**,经两个跨后端安全杠杆(字符串键标记消融进共享 `_tag_key_a1` `bl` helper;`undefined`/`true`/`false`/`null` 哨兵改用 `movImm64` 立即数),产物 **20430864 → 20021264**——回到 footprint 前天花板之下、重开余量,经全 5 目标编译验证。特性/正确性:数组**回调方法族**(`reduce`/`reduceRight` + `slice`/`charCodeAt`)补全 v1.5.29 的一等**方法引用**集;**`Number.prototype.toLocaleString`** 与 **`Date.prototype.toLocaleString`/`toLocaleDateString`/`toLocaleTimeString`/`toUTCString`/`toDateString`**(en-US,经零 footprint shim);**`RegExp.prototype.toString`**/`String(re)`/`.unicode` 与无 `new` 调用的 `RegExp(...)`;**`Date.parse("invalid")`/`new Date("bad")`** 现得 `NaN`(此前 `0`——nan-int0 别名陷阱),两位年 `new Date(95,0,1)` 映射到 1995。(仍延后:完整 `fs` 深度——其特定 syscall codegen 在所有尝试布局下确定性触发潜伏布局非确定性 bug,现进行根因排查。)fixtures 208。报告见 `tests/es-compat/`。)

## v1.5.29(**一等公民数组/字符串方法引用 + 完整 symbol 键语义 + Error/class 正确性 — gen2==gen3 逐字节一致,fixtures 199/201**。头条:**17 个数组/字符串方法现为一等值**(`typeof [].map === "function"`、`const f = arr.push; f.call(arr, 9)`、`[].map.call([1,2], x=>x*2)`、`.apply`、脱离引用、闭包捕获回调)——经通用接收者绑定 trampoline + 值读拦截,对既有静态方法调用派发纯增量(数组 push/pop/reverse/at/indexOf/map/filter/forEach/some/every;字符串 toUpperCase/toLowerCase/trim/slice/substring/at/includes/charAt/indexOf)。建于 v1.5.27 footprint 余量之上,经**权威主目录门**验证。**Symbol 现为一等键:** `sym in obj`、经计算字面量 `{[sym]:v}` 的 `obj[sym]` 读、symbol 键从 `Object.keys`/`values`/`entries`/`for-in`/`JSON` 排除、`Object.getOwnPropertySymbols`。**Error/class:** `class X extends Error{constructor(m){super(m)}}` 不再段错误并传播 `message`/`instanceof Error`/`name`/`cause`;`AggregateError(errors, msg)` 参数修正。另加 **`String.prototype.localeCompare`**(此前崩)、**`Array.from(arrayLike)`** 拷贝索引属性、**`structuredClone`** 现深拷(此前返回别名原对象)。(仍搁于 #32 对象即字典阻塞之后:原型的 `Object.prototype`/`getPrototypeOf` 身份半——已记的自举正确性险,非仅布局。)fixtures 199。报告见 `tests/es-compat/`。)

## v1.5.28(**Node + ES/number/closure 正确性 — 新 footprint 余量上 19 项修复,gen2==gen3 逐字节一致,fixtures 183/185**。迄今最大正确性批次,横跨 node 内建与 JS 核心,均经**权威主目录**自举门验证(产物 20086800,仍在 footprint 前天花板之下)。**Node:** `path` basename/dirname/normalize/join/`format`;`Buffer` `from(…,"hex"/"base64")` 解码 + `alloc(size,fill)`;`URL.pathname` 默认 + `URLSearchParams` sort/size/entries/pairs 构造;`querystring.unescape` 不再把 `+`→空格;`os.platform()` 返回 Node 名;`EventEmitter.defaultMaxListeners`;`util.inspect` depth + `util.callbackify`。**ES:** 非枚举属性现被 `Object.keys`/`values`/`entries`/`for-in`/`JSON.stringify` 跳过;引用外层变量的解构参数默认值(`({x=a})=>x`)正确读取。**Number:** `Number.prototype.toExponential`/`toPrecision`(shim,逐字节匹配 Node);非数字字符串算术(`"abc"-1`)现得 `NaN`(nan-int0 规范化,perf 中性);`255..toString(16)` 可解析;`String.prototype.replace`/`replaceAll` 展开 `$$`/`$&`/`` $` ``/`$'`。(再次延后——撞上本版布局的字节雷:完整 `fs` 深度 `statSync`/`readdirSync`;已验证实现留待未来布局。)fixtures 183。报告见 `tests/es-compat/`。)

## v1.5.27(**编译器 footprint −288 KB(尺寸天花板抬升)+ async/解构/Object.is 正确性 — gen2==gen3 逐字节一致,fixtures 160/162**。头条是基础设施:编译器此前在**每个** object/array 装箱点内联发射 8 指令 NaN-box 序列、在**每次**属性访问发射 2 指令字符串键标记(自举中约 32k 站点)。二者现均消融为共享 `bl` 运行时 helper(`_box_obj_r`/`_box_arr_r`/`_tag_str_a1`),回收 **~288 KB**,产物 **20201488 → 19906576**——**抬升了此前阻塞延后工作的布局尺寸天花板**,并缩小所有未来对象/数组/属性密集代码。经**权威主目录门**验证(worktree 门对布局敏感的自举非权威)。另加 6 项正确性修复:**async** — 箭头/函数表达式带参调用不再 SIGBUS,全部参数(1–5)正确传入 async 闭包/声明;**解构** — `{[k]:v,...rest}` 从 `rest` 排除计算键;**闭包** — 遮蔽外层变量的解构参数不再 SIGSEGV;**`Object.is(-0, 0)`** 现返回 `false`(带符号零 SameValue);**`new.target`** 现可解析(求值 `undefined`)。fixtures 160。报告见 `tests/es-compat/`。)

## v1.5.26(**Object/class/BigInt 正确性 — 11 项修复,gen2==gen3 逐字节一致,fixtures 148/150**。跨 object、class、BigInt 面的正确性批次(均经合并树自举门确认)。**Object:** `obj.isPrototypeOf(x)`/`obj.propertyIsEnumerable(k)` 现可用;`Object.create(proto, descriptors)` 应用第二参数;`Object.hasOwn`/`hasOwnProperty` 对含数字索引的数组不再崩;`arr.concat(...spread)` 不再误编译并崩溃。**Class:** 不同作用域的同名类不再共用一个 `_classinfo_` 槽;类方法值上的 `.call`/`.apply`/`.bind` 正确派发(此前落入对象派发);箭头/函数表达式体内 `new <顶层类>()` 不再段错误;计算属性类方法 `[k](){}` 按运行时键值定义。**Number/BigInt:** `-0 === 0`/`-0 == 0` 现返回 `true`;BigInt 位运算结果不再打印为 `Map`/`Set`/`Symbol`;`bigint.toString(radix)` 返回正确数字(此前 `"0"`)。fixtures 148。报告见 `tests/es-compat/`。)

## v1.5.25(**`process.env` + Buffer/crypto 扩面 — fixtures 148/150,仅剩 2 项失败**。合并树 gen2==gen3 逐字节一致(3 次干净运行验证——改动含启动 asm)。**`process.env`** 现可用:运行时在启动时解析 POSIX `envp` 数组为 `process.env` 的 `KEY=VALUE` 字符串(GC 安全),故 `process.env.PATH`、`process.env.MY_VAR` 等正确读取——fixtures 失败数 **3 → 2**(仅剩 2 个架构上 AOT 不兼容的循环 `require`)。另加 **`Buffer.copy`/`Buffer.compare`**、真实熵支持的 **`crypto.randomBytes`/`randomUUID`/`randomInt`**(经 `getentropy`/`getrandom`)、及 `util`/`timers/promises`/`assert`/`querystring`/`events` 扩面。(延后至后续 footprint 压缩:`fs.statSync`/`readdirSync`、`os.networkInterfaces`、`path.posix`/`win32`——各越过产物正撞上的 ~16KB 自举布局边界;已验证实现留存于历史。)fixtures 148。报告见 `tests/es-compat/`。)

## v1.5.24(**async/Promise 值正确性**。合并树 gen2==gen3 逐字节一致,fixtures 143。严格限于 Promise 组合器与值流(微任务调度器/协程核未动):**`await` 非 promise 不再段错误**(`await 7` 曾崩——现返回该值)、**`p.then(onFulfilled, onRejected)`** 尊重第二个拒绝回调(此前第二回调被忽略,故 `Promise.all(...).then(ok, err)` 从不传播拒绝——现已修)、**`Promise.any`** 全拒绝时产生正确的 **`AggregateError`**(带 `.errors`)、**async 箭头/函数表达式内未捕获 `throw`/await 拒绝拒绝其 promise** 而非进程退出。另加 **`Object.getOwnPropertyDescriptors`**。(超范围已记:`.then` 处理器内 `throw` 与 async **方法**需调度器/协程核改动。)fixtures 143。报告见 `tests/es-compat/`。)

## v1.5.23(**`Reflect`、`[...map]` 修复、`Object.fromEntries(iterable)`、`structuredClone`**。合并树 gen2==gen3 逐字节一致,fixtures 143。**`Reflect.get`/`set`/`has`/`deleteProperty`/`ownKeys`/`getPrototypeOf`/`defineProperty`/`apply`** 现可用(此前 undefined)。关闭两个长期延后缺口:**`[...map]`/`Array.from(map)`** 曾产生未装箱 `[k,v]` 配对、对元素调方法崩(`[...m].map(e=>e.join('='))`)——配对现正确装箱,故对 Map 的展开/`Array.from`/解构/`new Map([...m])`/for-of 全可用;**`Object.fromEntries(Map/Set/iterable)`**(不止数组)现经先展开可迭代对象而可用。另加 **`structuredClone(x)`** 深拷 JSON 安全数据(独立副本;undefined/函数/Date/Map/Set/循环有记录偏差)。fixtures 143。报告见 `tests/es-compat/`。)

## v1.5.22(**类特性轮**。合并树 gen2==gen3 逐字节一致,fixtures 143。补齐类支持缺口(私有字段/方法/访问器、静态块、`#x in obj`、字段初始化顺序已可用):**静态字段自引用**(`class C { static a=2; static b=C.a*10 }`——类名现在静态初始化**之前**绑定,此前崩)、**静态方法中的 `super`**(现解析到父**类对象**而非父原型)、**类表达式**(`const C = class {…}` 匿名与具名——此前不解析/崩;别名、传递、`new`、`extends UserClass`、循环内全可用)、**计算字段名**(`class C { [k] = v; static [expr] = v }`——解析与运行时键求值)。剩余类缺口(已记):变量键的计算**方法**名、`class extends BuiltIn`、同名跨作用域 `_classinfo_` 冲突。fixtures 143。报告见 `tests/es-compat/`。)

## v1.5.21(**自定义可迭代协议 + `toString(arg)` 分派**。合并树 gen2==gen3 逐字节一致,fixtures 143。**所有迭代点现驱动用户 `[Symbol.iterator]().next()`**:`for…of`、展开 `[...obj]`/`f(...obj)`、**`Array.from(obj)`**、**`new Set(obj)`**、**数组解构**(`let [a,b] = mySet`、`let [x,y] = gen()`、`let [[k,v]] = map`)全部尊重自定义迭代器(此前对对象越界读 → 空/垃圾/崩)。且 **`obj.toString(arg)` / `buf.toString("hex"|"utf8"|"base64")`** 不再误入数字进制路径——带用户 `toString` 方法的装箱对象以参数调用,`(255).toString(16)` 与 bigint 进制仍正常。(下一版待收:一批 Node `fs` 深度在集成时触发布局位置自举误编译——正在做 gate-safe。)fixtures 143。报告见 `tests/es-compat/`。)

## v1.5.20(**Math 三角 + 生成器/迭代器 + Node `fs`/syscall — fixtures 143/146**。第七次并行 agent 三 lane 收割,合并树 gen2==gen3 逐字节一致——即便协程布局改动与 arm64 asm/syscall 改动同时合入。**Math** 补齐三角族(`sin`/`cos`/`tan`、`asin`/`acos`/`atan`/`atan2`、`sinh`/`cosh`/`tanh` 及反函数、`fround`、`clz32`),经 fdlibm 风格核约 1 ulp 对齐 node;`String.replaceAll(regex,…)` 现走正则引擎(其已支持命名组、后行断言、反向引用)。**生成器/迭代器**:`{ *m(){} }` 简写可解析,生成器/方法的 **`this` 现正确绑定**(协程帧修复),**自定义 `[Symbol.iterator]`** 驱动 `for…of`/展开/`f(...obj)`。**Node**:真实 **`__syscall` macOS carry-flag 修复** 解锁 `fs.existsSync`/`mkdirSync`/`rmdirSync`(`statSync` 随后),**`require("node:X")`** 现返回默认导出以对齐 `import`(故 `require("node:events")` 给出可 new 的 `EventEmitter`),**`crypto.createHash`** 落地——fixtures 从 **140 → 143**。fixtures 143。报告见 `tests/es-compat/`。)

## v1.5.19(**类型化数组 + `console.log` 对齐 `util.inspect`**。第六次并行 agent 收割,合并树 gen2==gen3 逐字节一致,fixtures 140。**完整类型化数组**(Int8/16/32、Uint8/16/32/Clamped、Float32/64):从长度/数组/另一类型化数组构造(按类型强制:Uint8 回绕、Clamped 钳制、Float 存 double)、元素读写、`.length`/`.byteLength`/`.BYTES_PER_ELEMENT`、静态 `from`/`of`、迭代(`for…of`、展开、`Array.from`)、方法 `map`/`filter`/`reduce`/`forEach`/`join`/`indexOf`/`includes`/`at`/`slice`/`subarray`/`fill`/`set`/`reverse`/数值 `sort`——全部对齐 node。且 **`console.log` 现逐字节对齐 node `util.inspect`**:嵌套对象/数组(`{ a: 1, b: [ 2, 3 ] }`)、单引号字符串、`Map(2) { 'a' => 1 }`/`Set`、Date(ISO)与 Error——修复 `console.log(new Date())` **段错误**与遍地 `[object Object]`。另加 `String.lastIndexOf(s, fromIndex)`。(延后:类型化数组 `buffer`/DataView 多视图别名;`sort` 比较器。)fixtures 140。报告见 `tests/es-compat/`。)

## v1.5.18(**Node 核心模块扩面 + 引擎 x64 对等 — fixtures 140/143**。第五次并行 agent 收割,合并树 gen2==gen3 逐字节一致。Node 兼容性经新增/扩充核心模块拓宽(均经 `import` 对 node 验证):**`assert`**(ok/equal/strictEqual/deepEqual/throws)、**`querystring`**(parse/stringify/escape,与 node 逐字节一致)、**`url`**(`URL` 含 protocol/host/port/origin + 完整 `URLSearchParams`),另有 **`path`**(修 `normalize` 返 `[object Object]`;`sep`/`delimiter` 为字符串属性)、**`os`**(`EOL`/`devNull`)、**`Buffer`**(`byteLength`、健壮 `concat`)、**`events`**(`off`/`prependListener`)、**`util`**(`format` 尾参、node 风格 `inspect`、`promisify`、`isDeepStrictEqual`)扩面。fixtures 从 **133 → 140**(+7)。引擎侧,route-B `eval` 达成 **x64 对等**——HOST_DATA 重定位移植(`lea`→`mov` opcode 翻转)、片段按运行架构编译,故 `eval`/`new Function`/闭包/语句在 **macos-x64**(Rosetta 验证)与 arm64 同样可用。(已知后续:`require("node:X")` 对少数 builtin 的互操作落后于 `import`。)fixtures 140。报告见 `tests/es-compat/`。)

## v1.5.17(**引擎 `eval` 在 arm64 上功能完备 + ES 第三轮**。第四次并行 agent 收割,合并树 gen2==gen3 逐字节一致,fixtures 稳定 133。route-B `eval` 引擎新增**语句/控制流覆盖**(`try/catch`、`throw` 被调用方捕获、`switch` 贯穿、`for-of`/`for-in`、`do-while`、带标签 `break`/`continue`、函数声明提升)、**`new Function` 默认/剩余参数**(闭包包裹——`new Function("a","b=10","return a+b")(5)`→15、`("...xs","return xs.length")(1,2,3)`→3),以及最后的实用限制——**多页片段**(arm64 <4096B 单页帽经逐引用 ADRP 页差提升到 256KB,深 `.map().filter().reduce()` 链可 eval)。ES 侧:**`console.log` 打印含非数字元素的数组曾段错误**(每个字符串/布尔/null 元素被当指针解引用——现正确分派);**字面量 `NaN` 曾为真**(`if(NaN)`/`Boolean(NaN)`/`NaN?:` 全错——裸 double 自 `fcmp` 令其为假);**`Object.is({},{})`** 误返 `true`;**`Object.assign(t, ...sources)`** 丢展开源;**无参 `"x".split()`** 拆成字符而非 `["x"]`。fixtures 133。报告见 `tests/es-compat/`。)

## v1.5.16(**Node.js 兼容性飞跃 — fixtures 105 → 133**。第三次并行 agent 收割;Node 兼容 lane 与引擎、ES lane 合并,合并树上重验 gen2==gen3 定点逐字节一致。fixtures 从 **PASS=105/FAIL=31** 升到 **PASS=133/FAIL=3**(+28)。现已支持:**CommonJS `require()`**(AOT 子集——含 CJS 标记且无 ESM 语法的文件在读取时包裹 `module`/`exports`/`__filename`/`__dirname`;静态 specifier 的 `require` 目标加入模块图)、**`node_modules` + `package.json` 解析**(尊重 `exports` root/subpath/`*` 通配/条件、`main` vs `module`、`type`)、**`node:` scheme 及子路径**(`node:fs/promises`)、**ESM↔CJS 互操作**(import CJS 默认/具名绑定;从 `module.exports` 合成具名导出)、丰富的全局 **`process`**(`nextTick`/`version`/`versions`/`env`)、**`util.types`**,及一处真实 `fs.openSync` bug 修复。剩余 3 个 fixture 失败为 `require` 循环(无 lazy CJS 运行时时与 AOT 冲突)与启动 `envp` 的 `process.env`(定点风险),均刻意延后。本次也含更早 lane 的全局 `eval()`/`new Function()`。fixtures 133。报告见 `tests/es-compat/`。)

## v1.5.15(**`eval()` 现可在真实程序中调用 + 更多 ES 修复**。第二次并行 agent 收割,合并树上重验 gen2==gen3 定点,fixtures 105。**编译后的程序现能直接调用全局 `eval(str)` 与 `new Function(names…, body)()`**——经 shim 注入路由到 route-B 引擎(自举源码两者皆无,零定点影响):`eval("[1,2,3].map(x=>x*2).join(\",\")")`→`"2,4,6"`、`eval("var x=5; x*x")`→25、`new Function("a","b","return a+b")(2,3)`→5,含形参绑定、函数返回语义、闭包、可捕获的语法/超尺寸错误。ES 侧:**`String.prototype.search(regex)`** 崩;**`Object.defineProperties`** 崩(现脱糖为 `defineProperty`);**原始值 `.valueOf()`**(`(42).valueOf()`)崩(被当 Date 方法分派);**`Map.get(缺失键)`** 返 `0` 而非 `undefined`;**`{} == {}` / `[] == []` 误返 `true`**(对象 `==` 按规范是引用相等)。fixtures 105。报告见 `tests/es-compat/`。)

## v1.5.14(**并行 agent 波:引擎 `eval` 抵达闭包 + 更多静默 bug 修复**。两个 agent 在隔离 worktree 并行——一个攻 route-B 引擎,一个攻 ES 正确性——集成时在**合并后的树**上重验 gen2==gen3 定点(fixtures 稳定 105)。**route-B `eval` 引擎现能跑真实代码**:数组/对象字面量、字符串/数组方法、`typeof`、`length`(全走宿主共享堆)、**语句序列 + 局部 `var`/`let`/`const`**(`eval("var x=5; x*x")`→25),以及重头戏——**函数/箭头表达式与闭包**(`eval("[1,2,3].map(x=>x*2).filter(...).reduce(...)")`、闭包捕获外层 eval 局部、嵌套闭包),经新的片段内代码 label 重定位。引擎唯一剩余边界是 arm64 <4096B 单页片段限制(长算子链,干净报错)。ES 侧:**`str.replaceAll(regex, repl)`** 曾是空操作(分派漏了 `replaceAll`);**`Math.imul`** 崩(现实现);**`Number("+5")`** 及一切前导 `+` 串返 `NaN`;**`(n).toString(radix)`** 对 ≥2³¹ 整数回绕(`(0xdeadbeef).toString(16)`→`"deadbeef"`)。另加本批早先:对象方法名撞 `pop`/`push` 在 unknown 接收者上崩、`Object.fromEntries` 丢数值键。fixtures 105。报告见 `tests/es-compat/`。)

## v1.5.13(**静默 bug 猎杀波**。对 Node 差分测试扫出并修复一串高频静默腐蚀,每处均 gen2==gen3 定点保持、fixtures 稳定 105。**`obj[缺失键]` 现返 `undefined` 而非 `0`**——旧的 `0` 破坏 `??`、`typeof`、`===undefined` 及一切记忆化/可选字段模式(`c[n] ?? (c[n]=f(n))` 返 `0`)。**成员赋值求值为被赋的值**:`(o[k]=v)`、`return o.x=v`、`f(a[i]=v)` 此前返调用残留(键)。**松散 `!=` 倒置**——`x != null` 恒等于 `x == null`(仅 `!==` 走另路,掩盖了它)。**`Array.indexOf`/`lastIndexOf` 按值比字符串**而非指针,故 `"a-b-c".split("-").indexOf("b")` 与动态构造串命中。**越界 `a[i]=v` 空档为 `undefined`** 而非 `0`。另:`i*i`/`i+i` 归纳变量乘积未装箱(`push(i*i)`→`"0."`)、用户类 `get size()` 被 Map/Set `.size` 分派遮蔽、`new Set("hello")` 崩、`var x = o.p = 10` 不解析、嵌套 `class E extends D` 段错误。引擎(route B)eval 亦扩到比较/位运算/三元/Math/混型 + 首个共享堆重定位。fixtures 105。报告见 `tests/es-compat/`。)
