{
  "description": "arrow with object-destructuring param whose names shadow outer let vars must bind the params, not capture/segfault",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "30",
    "exitCode": 0
  }
}
