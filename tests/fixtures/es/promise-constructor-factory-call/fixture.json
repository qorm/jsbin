{
  "description": "new Promise(executor) should support direct factory calls that statically return an executor",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "31",
    "exitCode": 0
  }
}
