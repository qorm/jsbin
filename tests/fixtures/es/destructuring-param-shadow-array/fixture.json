{
  "description": "arrow with array-destructuring param shadowing outer let vars binds the params correctly",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "12",
    "exitCode": 0
  }
}
