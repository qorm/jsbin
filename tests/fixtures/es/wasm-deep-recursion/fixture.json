{
  "description": "Deep recursion (depth 800) succeeds under both native and wasm32-wasi; regression guard for the enlarged wasm shadow stack",
  "expect": { "parse": true, "compile": true, "run": true, "stdout": "320400\n0", "exitCode": 0 }
}
