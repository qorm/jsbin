{
  "description": "Promise.race should resolve from the first already-settled element",
  "expect": {
    "parse": true,
    "compile": true,
    "run": true,
    "stdout": "7",
    "exitCode": 0
  }
}
